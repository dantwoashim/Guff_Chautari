# Self-Hosting Overview

Primary deployment guide:
- `../self-host-guide.md`

Quick commands:
```bash
./scripts/self-host-setup.sh
npm run deploy:preflight
npm run deploy:up
```

This flow validates environment values, runs quality gates, and prepares the deploy image.
